<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hp.ali.stockmonk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StockMonk">
        <activity android:name=".TestActivity"></activity>
        <activity android:name=".FavoritePost" />
        <activity android:name=".FavoriteActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".login.ForgotPasswordActivity" />
        <activity android:name=".login.SignupActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ui.knowledge_base.PostDetailActivity" />
        <activity
            android:name=".login.SplashAcitivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.StockMonk.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>